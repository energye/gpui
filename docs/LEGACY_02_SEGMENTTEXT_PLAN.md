# 单行 5 万字余量：整行 `SegmentText`

> 顺序 2。前情：提交 `79c2efb` 行内复用把击键中位数 72ms→24ms，200ms 防退化绊线与 13 裁判全绿。

## 问题

剩下的约 24ms 卡在整行 `SegmentText`（UAX#9，在 `render/text` 层）：每敲一个字都要把整行扫一遍做双向分段，行越长越慢，超线性不收敛。

## 验收

- 单行 5 万字击键中位数目标以新会话真机 profile 核定（**改门禁数值需审批，禁单方面**，沿 §9 纪律）。
- `ui/rendering/m5_singleline_test.go` 200ms 绊线与 13 裁判全绿；`BenchmarkKeystroke` 单跑（计时单跑、禁并行）。
- 真窗不断：不断笔、不错位（R21 结论不受影响）。

## 方案步骤

1. 新会话先读本篇 + 规模计划 §9 该行 + `ui/rendering/row_reuse.go`，再执行。
2. 真机 profile 定热点分布（分段 vs 整形 vs 拼接各占多少），再定做前后缀复用缓存还是增量分段。
3. 动 `render/text` 层（ onset 与 `row_reuse.go` 的接缝保持一致），红灯起步：先写失败单测。

## 会话边界

新会话执行；GPU 真窗可跑任意场景窗验证不断笔；计时类测试一次只跑一个。

## 优化收敛

收尾做一遍：分段缓存命中率、内存（缓存键粒度）、冗余（与行内复用的重复扫描合并）、可读性。

## 原文档同步清单

- 规模计划 §9 该行追加落地结论；§10 加一行；`RENDER_API_CATALOG.md` 只有公开 API 变化才动。
