# accept Golden 基线刷新归档

> 顺序 3。**必须独立评审，禁顺手办**（裁判公正性事项）。

## 问题

验收拿来对比的 M0 基线图过期了：新截图与它差 1.0%，都是散布字形抗锯齿级像素。现在分不清是画坏了还是基线旧了，再拖下去真画坏也看不出来。

## 验收

- 以 M5 截图为新基线归档，accept 重跑差异归零。
- 独立评审确认 1.0% 确为抗锯齿级（逐位对照记录在案），评审结论写入 §10 行。
- 所有引用旧基线的窗/README 指到新基线，无悬空引用。

## 方案步骤

1. 新会话先读本篇 + 规模计划 §9 该行，再执行。
2. 重跑 accept 窗取新截图，逐位对照旧基线，输出差异图与分布说明。
3. 送独立评审（评审人确认抗锯齿级），通过后归档替换引用。

## 会话边界

新会话执行；纯文档+基线资产变更，**零代码改动**；GPU 真窗重跑 accept 确认。

## 优化收敛

无代码收敛；收尾做一遍引用一致性检查（grep 旧基线文件名零残留）。

## 原文档同步清单

- 规模计划 §9 该行记“已刷新归档+评审结论”；§10 加一行。
