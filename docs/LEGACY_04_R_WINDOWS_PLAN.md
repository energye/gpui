# R1/R15 两个单能力窗关闭（R22 已关）

> 顺序 4。§2 主表 R22 已 ✅，剩 R1/R15 两处 ⬜，建议按 R1→R15 从小到大一件一件关（可分两次会话）。

## 问题

- R22（选区/光标局部脏预留）：已关（`ui_wr_r22_selection_stub` 5s，✅），留档备查。
- R1（局部 NeedsLayout）：改一个字只布局脏子树的约定没窗锁着，改坏布局隔离没人拦。
- R15（UI/raster 长跑 300s）：窗已建（`ui_wr_r15_soak` 300s 首跑 PASS），次跑 Golden 待补，保持 ⬜。

## 验收（每窗）

- 独立包与 §2 一一对应（`ui_wr_r22_selection_stub` / `ui_wr_r1_layout` / `ui_wr_r15_soak`），空目录=未建不得标 ✅。
- `RUN_SECONDS` 按 §2.5（R22 5s，R1 5s，R15 300s）；R15 按 soak 口径采 CPU/RSS slope。
- U21 三证据（逻辑探针+像素断言+Golden），门禁写进各窗 README。
- R22 字段可 0、API 存在、空跑；R1 `layout_count` 只脏子树；R15 无崩、无读回。

## 方案步骤

1. 新会话先读本篇 + 真源 §2 该行 + §2.2/§2.5/§2.7，再执行。
2. 先查包在不在（在就复用补门禁，不在就新建），走 wr-close 首次关闭模式。
3. FAIL 项走 wr-debug 完整流程。

## 会话边界

新会话执行（R15 的 300s 单独一次会话）；GPU 真窗 1200×800。

## 优化收敛

每窗收尾做一遍：采样开销、探针与门禁去重、可读性。

## 原文档同步清单

- §2 该行 ⬜→✅；§10 每窗一行；§5 波次照改。
