# C10 长跑组合窗新建

> 顺序 5。**前置：R15 关闭后**（C10 跑 R15+全主路径 300s 长跑）。

## 问题

单能力长跑（R15）绿了不代表拼在一起也绿：C10 是全主路径 300s 组合长跑，验证长期运行下各路径之间不互相拖累（无崩、hitch 可报）。§3 无状态，即未建。

## 验收

- 新建 `examples/ui_wr_c10_soak`（1200×800·300s），与 §3 一一对应。
- 无崩、无读回；hitch/CPU/RSS 按 §2.2 长窗口径判（阈值写窗 README）。
- U21 三证据；只做集成，不代替任何 R。

## 方案步骤

1. 新会话先读本篇 + 真源 §3 C10 行 + §2.5 长窗档，再执行（确认 R15 已 ✅）。
2. 走 wr-close 组合窗模式；FAIL 走 wr-debug 完整流程。

## 会话边界

新会话执行；GPU 真窗；300s 独占一次会话。

## 优化收敛

收尾做一遍：长跑采样开销、内存 slope、探针复用、可读性。

## 原文档同步清单

- §3 C10 补状态；§10 加一行。
