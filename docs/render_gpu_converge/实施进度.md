# render/gpu 收敛实施进度（四批）

> 日期：2026-09-18（用户睡中托管执行，不提交，等醒后确认）。
> 范围纪律：只改 `render/` 与 `gpu/`（目录文档 `docs/RENDER_API_CATALOG.md` 按 API 同步纪律同步）；
> `game/`、`ui/`、`video/`、`examples/` 逻辑未动（示例只跑验证窗，未绕引擎洞）。
> 金图口径：追车窗 `chase_final.png diff=0.0000% / 33400px`、最小窗 `game_quad parity=0 golden=0`；
> 真窗 FAIL 行里的 `presents<N want>=6500 (S52)` 是 S52 长跑门限（25s 窗跑不满 6500 帧），非本批回归项。

## 第一批（depth 吞错 / MSAA 1x / 120 帧闩锁）—— 已过

改了啥文件：
- `gpu/rwgpu/device.go`：`CreateDepthTextureErr` 已在工作区（报原生错），`CreateDepthTexture` 转调它（`t, _ := ...Err`），吞错只留兼容壳。
- `gpu/rwgpu/texture_test.go`：`TestCreateDepthTexture` 改走 `CreateDepthTextureErr`（断 err + 非 nil）。
- `gpu/rwgpu/null_guard_test.go`：nil 设备补 `CreateDepthTextureErr` 子项（nil+有错），保留老 `CreateDepthTexture` 子项。
- `gpu/rwgpu/examples/cube/main.go`：`createDepthTexture` 改走 `CreateDepthTextureErr`（`%w` 包错）。
- `render/internal/gpu/gpu_textures.go`：B1 未动（`sc:=1` 默认、`degradedReprobeFrames=120`、`degradedServe/latch/clear` 已在 966ac82 落地，注释与实现一致）。
- `render/internal/gpu/oom_fallback_test.go`：新增 `TestTextureSet_DegradedLatch`（120 帧窗：窗内服务、到期只重探一次、异尺寸旁路不耗窗、`clearDegraded` 生效；1200x800 + 800x600 双尺寸验证钥匙按尺寸分窗）。

单测结果（逐个跑）：
- `gpu/rwgpu TestNullGuard_Device_Creation`（含新 `CreateDepthTextureErr` 子项）：PASS。
- `gpu/rwgpu TestCreateDepthTexture`：FAIL（环境性：`wgpu: native library not loaded` 无头单测无 native；stash 原版同 FAIL，已证非本批引入）。
- `render/internal/gpu TestTextureSet_FailFastLatch`：PASS；`TestTextureSet_DegradedLatch`：PASS；`TestCreateTextureRetryOOM_SmallHeapInjection`：SKIP（无小堆 fixture，有原因，非静默绿）。
- `gofmt` 干净；`go vet ./gpu/rwgpu/` 干净；`go vet ./render/internal/gpu/` 仅 3 处存量警告（`gpu_render_context.go:3894/3942 unreachable`、`view_raw_attack_test.go unsafe.Pointer`，本批未碰该文件）。

真窗 JSON 数：
- 追车窗 iGPU（默认）：`probes ok=true parity=0.0000% golden wrote=false changed=0 fallback=0 fps≈58.5 hitch≈7.2/min`。
- 追车窗 `GPUI_POWER=high`：独显 OOM 走降级链 `high→low`，落 `gpu_backend=integrated fallback=1`，探针/像素/金图同绿。
- 最小窗 iGPU / `GPUI_POWER=low`：`OK parity=0 golden=0`（两次皆绿）。
- 金图对比：`chase_final.png diff=0.0000% / 33400px`；`game_quad golden_changed_pct=0`。追车快照属运行产物，已 `git checkout` 复原。

遗留红：`TestCreateDepthTexture` 环境性 FAIL（见上）；追车 `presents≈1481 want>=6500 (S52)` 系长跑门限（25s 窗），非回归。

## 第二批（purge 全链 / 绑定组视图身份 / 台账记全）—— 已过（含 1 个存量红修后转绿）

改了啥文件：
- `render/internal/gpu/gpu_textures.go`：`createTextureRetryOOM` 失败路补第二级 `render.PurgeAcceleratorSurfaceResources()`（session MSAA/depth + `drainOffscreenPool` + texturePool + pending 双 Drain）再试一次；非 OOM 直接返回（不吞错）。
- `render/internal/gpu/render_session.go`：文本绑定组补 `textBGViews` 视图身份键（与 image `imageBGViews` / 字形 `glyphMaskBGViews` / gpuTex `gpuTexBGSlotCache` 同规则：命中复用、失配 deferred-release 重建；`invalidateTextBindGroups` 与 teardown 同步清键）。
- `render/internal/gpu/opt27_gputex_bg_cache_test.go`：`TestOpt27_GPUTexBGSlotCache_ReusesView` 前置 `ensurePipelines()` 改 `ensureImagePipeline()`（旧前置不断言 image 管线，本机必 panic，属存量用例 bug，修后转绿）。
- `gpu/rwgpu/vram_ledger.go`：台账记全管线/采样器/交换链（`vramPipelineBytes=64K`、`vramSamplerBytes=4K`、`vramSurfaceBytes(w,h)=单帧 BGRA8`；`Surface.Configure` 记账/`Unconfigure`/`Release` 退账，重 Configure 替换不 double-count；`VramBudgetMB`/`VramTestReset`/`VramTestAdd` 导出）。
- `gpu/rwgpu/sampler.go`、`pipeline.go`（compute）、`render_pipeline.go`：Create 记账 + Release 退账。
- `gpu/rwgpu/surface.go`：同上（Configure 记账、Unconfigure/Release 退账）。
- `gpu/rwgpu/vram_ledger_test.go`：新增 `TestVramLedgerNonTextureKinds`（采样器/管线/表面四类记账+退账+重 Configure 替换不断言 double-count）。

单测结果（逐个跑）：
- `gpu/rwgpu` 台账四件（ChargeRefund/BudgetGate/NonTextureKinds/TextureBytes）：全 PASS。
- `render/internal/gpu`：`TestS67_ImageCache_HitUploadBytes/EphemeralReleased`、`TestS67_TexturePool_HitMissStats`、`TestStencilRendererEnsureTextures`、`TestSDFRenderPipelineTextures`、`TestTextureSet_FailFastLatch/DegradedLatch` 全 PASS；`TestOpt27_GPUTexBGSlotCache_ReusesView` 修后 PASS。
- 遗留红：`TestOpt27_BuildGPUTextureResources_MultiViewBGCache` 仍 FAIL（`create gpu texture vertex buffer: Validation Error … group:1 binding:0 missing from pipeline layout`，与本批绑定组键改动无关的管线布局错配；B2 未扩散修它，留待管线线处理，不进金图门）。

真窗 JSON 数：追车 iGPU `probes ok=true parity=0 golden changed=0 fallback=0`；`GPUI_POWER=high` 照旧 `high→low` 落核显 `fallback=1`；最小窗 `OK parity=0 golden=0`。
金图对比：`chase_final.png diff=0.0000% / 33400px`；quad `golden_changed_pct=0`。快照产物已复原。

## 第三批（探针并共享设备 / 判死合并）—— 已过

改了啥文件：
- `render/present_target.go`：新增导出 `AcquireSharedPresentDevice()`（只读共享设备问询，不拿 refcount，Release 归 PresentTarget.Close）；`waitDeviceReady` 注释并入统一 OOM 政策说明。
- `render/internal/gpu/gpu_shared.go`：`initGPU` 先借活着的共享呈现设备（同设备 N 表面，不再开第二设备）；无共享时才按 `ResolveAdapterPolicy()`（GPUI_POWER）选卡开独立设备；`forceFallback` 纳入 software 判定。
- `render/oom_exit.go`（新）：`OOMExitThreshold=3` + `OOMExit`（Note/RunErr，nil 接收者惰性）为唯一判死实现。
- `render/oom_exit_test.go`（新）：`TestOOMExit_SingleAuthority`（3 连 OOM 退出、非 OOM 重置、nil 惰性）。
- `ui/embedder/oom_exit.go`：瘦身为 `render.OOMExit` 委托（阈值/文案/实现全部同源，调用点不动）。

单测结果（逐个跑）：`render TestOOMExit_SingleAuthority / TestResolveAdapterPolicy / TestAdapterPolicyString`、`ui/embedder TestOOMExit_StreakAndLatch` 全 PASS。
`gofmt` 干净；`go vet ./render/ ./ui/embedder/` 干净；`./render/internal/gpu/` 同 B1 存量 3 警告。

真窗 JSON 数：追车 iGPU `probes ok=true parity=0 golden changed=0 fallback=0`；最小窗 `OK parity=0 golden=0`。
策略探针（Vulkan）：`default→Intel iGPU`、`high→940MX dGPU`、`low→Intel iGPU`（与窗口 `gpu_backend=integrated` 一致）。
金图对比：`chase_final.png diff=0.0000%`；quad `golden=0`。快照产物已复原。

遗留红：同 B1/B2（`TestCreateDepthTexture` 环境 FAIL、`MultiViewBGCache` 管线布局 FAIL、S52 presents 门限）。

## 第四批（水位自动 LowVRAM / 目录 DrawShapedGlyphs）—— 已过

改了啥文件：
- `gpu/rwgpu/vram_ledger.go`：`VramBudgetMB` 已在 B2 导出（本批复用）。
- `gpu/webgpu/vram.go`（新）：`VramLiveBytes/VramBudgetMB` 转发（render 读一数，不直 import rwgpu）。
- `gpu/webgpu/vram_test_api.go`（新）：`VramTestReset/VramTestAdd` 测试转发。
- `render/vram_ledger_forward.go`（新）：render 侧 `vramTestReset/vramTestAdd` 转发。
- `render/adapter_policy.go`：`DeviceDescriptorForAdapter` 加台账水位自动 LowVRAM（live ≥ 80% 预算即紧限，`GPUI_LOW_VRAM_WATERLINE_PCT` 1–100 可调，预算 0=禁用永不触发；`GPUI_LOW_VRAM=1` 照旧强制；核显/CPU 自动紧限保留）。
- `render/adapter_policy_test.go` + `render/vram_test_hook_test.go`（新）：`TestLowVRAMWaterline_AutoTrip`（0/79/80MB 对 100MB 预算三档 + 50% override + 禁用预算永不触发，纯 Go）。
- `docs/RENDER_API_CATALOG.md`：`DrawShapedGlyphs` 由“🧪仅测试”改“🔗生产在用”，§3.8 行 + §7.2 行同步 6 处调用证据（`ui/rendering/text.go:988/1077`、`ui/rendering/text_picture.go:205/232/244`、`ui/scene/picture.go:225`，另 `render/text.go:158` 与 `render/scene/gpu_renderer.go:247`）；§3.7 行同步 `OOMExit/AcquireSharedPresentDevice`/水位开关新增 API（含修订语）。

单测结果（逐个跑）：`TestResolveAdapterPolicy / TestAdapterPolicyString / TestPresentLevels_Order / TestLowVRAMWaterline_AutoTrip / TestOOMExit_SingleAuthority` 全 PASS。

真窗 JSON 数：追车 iGPU `probes ok=true parity=0 golden changed=0 fallback=0`；最小窗 `OK parity=0 golden=0`。
金图对比：`chase_final.png diff=0.0000% / 33400px`；quad `golden_changed_pct=0`。快照产物已复原。

遗留红：同前（`TestCreateDepthTexture` 环境 FAIL、`MultiViewBGCache` 管线布局 FAIL、S52 presents 门限）。

## 全批未提交清单（等用户醒后确认再提交）

B1：`gpu/rwgpu/device.go`、`gpu/rwgpu/texture_test.go`、`gpu/rwgpu/null_guard_test.go`、`gpu/rwgpu/examples/cube/main.go`、`render/internal/gpu/oom_fallback_test.go`。
B2：`render/internal/gpu/gpu_textures.go`、`render/internal/gpu/render_session.go`、`render/internal/gpu/opt27_gputex_bg_cache_test.go`、`gpu/rwgpu/vram_ledger.go`、`gpu/rwgpu/sampler.go`、`gpu/rwgpu/pipeline.go`、`gpu/rwgpu/render_pipeline.go`、`gpu/rwgpu/surface.go`、`gpu/rwgpu/vram_ledger_test.go`。
B3：`render/present_target.go`、`render/internal/gpu/gpu_shared.go`、`render/oom_exit.go`、`render/oom_exit_test.go`、`ui/embedder/oom_exit.go`。
B4：`render/adapter_policy.go`、`render/adapter_policy_test.go`、`render/vram_ledger_forward.go`、`render/vram_test_hook_test.go`、`gpu/webgpu/vram.go`、`gpu/webgpu/vram_test_api.go`、`docs/RENDER_API_CATALOG.md`。
工作区另有他线未提交改动（`docs/ENGINE_THREAD_T_PLAN.md`、`docs/ENGINE_VIDEO_DECODE_PLAN.md`、`video/h265/*`、`examples/ui_wr_c4_shell_overlay/golden/*` 等）——按仓库纪律未碰、未列入本批提交清单。
