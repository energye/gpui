// Package wrkit is the shared quality-bar kit for ENGINE_UI_WIDGET_RENDER
// real windows (LiveHUD, phase clock, RUN_SECONDS helpers).
//
// Scope: examples only. Do not import wrkit from ui/ or use it to paper over
// engine holes — those go through wr-engine.
package wrkit
