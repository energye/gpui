## git 提交纪律（硬）

- **只提交自己本次改动的文件**：用 `git status` 对照改动清单，只 `git add` 本次会话我创建/修改的文件；工作区里其他线的工作（其他会话/他人遗留的未提交改动）**禁止一并提交**、禁止 `git add .` / `git add -A` / `git commit -am`。
- 提交前先和用户确认, 然后 `git diff --stat` 确认清单，若发现无关改动，分文件提交并向用户说明哪些留给了哪条线。
- 未推送的本地提交不算完成同步；推送动作需用户确认。

## 技能执行规则

- 所有 skill 的「停下报告」节点必须停，用 `question` 工具问用户确认方向后再继续，禁止跳过或自决。
- 禁止在示例层绕引擎洞（改 `examples/` 而不改引擎层）；示例层只做验证，不做绕过。
- 门禁绿 ≠ 底层洞已修；洞修了才能标绿。
- **所有分层按同一标准处理**：在能力实际所在位置直接实现/修复，修复前照常做影响面评估并停在「停下报告」节点征求方向确认，确认后即可动手，无需额外分级审批。
- 底层不满足 → 交底层 skill（禁止在示例里绕）；调度型 skill 不直接写代码。

## 真窗与状态真源（硬）

- **状态只认各域真源文档的主表 + 组合表 + 分期 + 修订**（具体哪份见 `docs/`，例如部件域、窗口域、渲染域各有其总账）。
- 每个主能力必须有独立真窗；**禁止**用组合窗代替单个能力；能力对应的 example 以真源的实现标准为准。
- **像素级断言规范（硬）**：一切可见图形必须断言「画面对」——形态选型、Golden 逐位回归对比、容差显式声明、三证据合证（逻辑探针+像素断言+Golden 缺一不可）。只有逻辑指标绿 = 假绿风险，禁止标绿。
- 空目录（无 main.go）= 未建，不得标绿。

## 修复流程纪律

- 任何能力点 FAIL → 必须加载对应 skill 并严格走完该 skill 的完整步骤。
- skill 内标记「停下报告」的节点必须用 question 工具停下，得到用户确认后再继续。
- 禁止跳过 skill 中的任何一步直接修代码。

## 新问题归位与跨线解决纪律（硬）

- 当前实现遇到任何新问题，先落位：记入对应阶段/能力的子项，写清完整问题项与状态（现象、位置、与真值差异、归属阶段/能力、待办），不口头记、不跳过。
- 是当前线的，按优先级直接解决，解决完更新子项状态。
- 非当前线的，切换到对应线解决，直到解决完再回来；禁止把别线的问题晾着，也禁止顺手在当前线绕过去。

## 实现和修复流程纪律（硬约束）

- 渲染层新功能实现，统一对齐 Skia / Flutter 标准实现方案；
- 渲染层问题修复先核查 Skia/Flutter 是否存在原生支持能力，优先复用避免冗余；存量代码未对齐标准的需重构对齐；
- 所有渲染层变更需兼顾收敛性与性能优化，在保证功能正确基础上，提升代码可读性、扩展性、可维护性。

## 引擎层硬编码纪律（硬）

- **引擎层禁止写死用户可控变量**：引擎层禁止出现用户可控的文案/占位/业务值硬编码，除非是引擎内部自洽的默认值且通过可配置 API 暴露；
- 用户可控值必须通过 API 传入，引擎默认给空或内部安全默认值，示例/应用层显式设置；
- 回归检查：用户可控硬编码检查与掩码参数化检查，合入前必跑（具体命令见所属域真源）。

## 每次新上下文

- 先读 AGENTS.md（本文），再读 `docs/` 真源，再执行。
- 所有状态以真源为准，不凭记忆。

## API 文档同步纪律（硬）

- **公开 API 改动必须同步总账文档**：每次增加/修改/删除公开 API（顶层 func/type/const/var、导出方法、签名、接线状态），必须同步更新所属域的总账文档（分类表、统计、状态、速查、修订表）。
- **机器校验（合入前必跑）**：跑所属域的目录文档覆盖检查；非零退出 = 文档漏了，补完再合入。
- 状态标注必须可溯源：生产消费者 → 文件:行号；实测 → 真窗名/日期/现象。禁止凭印象标注。

## 跨平台标记纪律（硬）

- **同功能多端一次标齐**：每次改完不管做哪端，同一功能各平台/后端格一次写完，不许留空白格；没做就写明原因（占位/实现中/协议不可做），不许空着等以后补。
- **符号只用所属文档的图例**：另加预期走哪个系统接口；版本号加修订一起走，写清哪端做了哪端没做。
- **合入前必查**：同功能表按行扫一遍，有空白格就打回；只改一端时也要把其余端的状态顺手带上。

## 对话回答（硬，对所有对话生效）

- **回答一律用大白话**：不堆黑话、术语要用通俗话解释，像跟人聊天一样把事说清楚。适用所有对话、所有包。
- 给结论时先一句直话（是什么/行不行/差多少），再解释为什么。别上来就贴代码或指令。

## 测试数据与阶段回归（硬）

- **测试数据一律入 `testdata/`**；进程临时文件仅允许 `t.TempDir()`，禁止把外部绝对路径写死在测试代码里。
- 测试一律读数据文件，**禁止在测试代码里用循环/硬编码生成标准数据**；新增数据子集必须先落数据文件。
- 外部工具链不提交二进制；测试经环境变量或自动重建解析，禁止把本机绝对路径硬编码进测试。
- 每阶段必须回归之前所有已完结阶段，新阶段不得回退旧阶段。
- **回归按测试文件逐个跑，禁止一次全量**：对每个 `*_test.go` 文件单独执行，一个文件跑完（PASS/SKIP/FAIL 记录）再跑下一个文件；长跑文件单独给足 `-timeout`。
- 依赖外部数据的用例，数据缺失时用 `t.Skipf` 提示原因，**禁止静默假绿**。

## 当前环境（硬）

- 有真机环境时，可运行真窗测试。
- **真窗必须自测 + 人工二合一**：需人工看效果的真窗，先跑自动自检（断言 + 出 JSON，门禁用 `-auto-only` 直接判 PASS/FAIL），再常驻等人工操作（收真事件即打日志并改标题显示状态，关窗或 `-manual-seconds` 超时后出汇总 + JSON）；**跑完即关的一次性验收窗不能代替常驻人工窗**。

## 参考成熟框架设计与实现

- /home/yanghy/app/projects/flutter
- /home/yanghy/app/projects/gogpu/gio
